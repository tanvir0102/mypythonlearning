#!/usr/bin/env	python3

#constructing tuples.
t = (1,2,3)
print(len(t))
print(t[0])

#basic tuple methods.
print(t.index(2))
print(t.count(1))
t = (1,1,1,2,2,3,4)
print(t.count(1))

#Immutability.
#we can't assigin values in tuple
#When to use tuples.
